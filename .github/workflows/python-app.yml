name: 构建并发布可执行文件（PyInstaller）

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag_name:
        description: '指定tag（留空自动取最新）'
        required: false
        type: string
      create_release:
        description: '是否创建Release'
        required: true
        default: true
        type: boolean

env:
  PYTHON_VERSION: "3.10"
  PROJECT_NAME: "ad"

jobs:
# ========================
# 版本 + Changelog
# ========================
  meta:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.tag.outputs.tag }}
      version: ${{ steps.ver.outputs.version }}
      notes: ${{ steps.changelog.outputs.notes }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - id: tag
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            if [[ -n "${{ github.event.inputs.tag_name }}" ]]; then
              TAG="${{ github.event.inputs.tag_name }}"
            else
              TAG=$(git tag -l "v*" --sort=-v:refname | head -n1)
            fi
          else
            TAG="${{ github.ref_name }}"
          fi
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - id: ver
        run: |
          VERSION="${{ steps.tag.outputs.tag }}"
          VERSION=${VERSION#v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      # 使用 heredoc 方式输出多行 notes，避免转义问题
      - id: changelog
        run: |
          TAG="${{ steps.tag.outputs.tag }}"
          PREV=$(git tag --sort=-v:refname | grep -A1 "^$TAG$" | tail -n1)

          if [[ -z "$PREV" || "$PREV" == "$TAG" ]]; then
            NOTES=$(git log --pretty=format:"- %s" -n 20)
          else
            NOTES=$(git log "$PREV..$TAG" --pretty=format:"- %s")
          fi

          # 使用 heredoc 写入 GITHUB_OUTPUT
          EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
          echo "notes<<$EOF" >> $GITHUB_OUTPUT
          echo "$NOTES" >> $GITHUB_OUTPUT
          echo "$EOF" >> $GITHUB_OUTPUT

# ========================
# 构建（PyInstaller 稳定版）
# ========================
  build:
    needs: meta
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        include:
          - os: windows-latest
            name: windows
            cache_path: ~\AppData\Local\pip\Cache
          - os: ubuntu-latest
            name: linux
            cache_path: ~/.cache/pip
          - os: macos-latest
            name: macos
            cache_path: ~/Library/Caches/pip

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.meta.outputs.tag }}

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - uses: actions/cache@v4
        with:
          path: ${{ matrix.cache_path }}
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      # PyInstaller 打包为文件夹
      - name: PyInstaller 打包（非 onefile）
        shell: bash
        run: |
          OUT_NAME="${{ env.PROJECT_NAME }}-${{ matrix.name }}-${{ needs.meta.outputs.version }}"
          pyinstaller main.py \
            --name "$OUT_NAME" \
            --distpath build \
            --workpath build/temp \
            --specpath build/spec \
            --noconfirm \
            --console \
            --onedir

      # 打包目录为 zip
      - name: 压缩构建产物
        shell: bash
        run: |
          cd build
          NAME="${{ env.PROJECT_NAME }}-${{ matrix.name }}-${{ needs.meta.outputs.version }}"
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            powershell Compress-Archive -Path "$NAME" -DestinationPath "$NAME.zip"
          else
            zip -r "$NAME.zip" "$NAME"
          fi
          mv "$NAME.zip" ..

      - name: 生成 SHA256
        shell: bash
        run: |
          FILE="${{ env.PROJECT_NAME }}-${{ matrix.name }}-${{ needs.meta.outputs.version }}.zip"
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            # 修正 Windows 下的 hash 提取，并输出 "hash 文件名" 格式
            certutil -hashfile "$FILE" SHA256 > tmp.txt
            # 第二行是 hash 值，提取后与文件名拼接
            HASH=$(sed -n '2p' tmp.txt | tr -d '\r')
            echo "$HASH  $FILE" > "$FILE.sha256"
          else
            shasum -a 256 "$FILE" > "$FILE.sha256"
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.name }}
          path: |
            *.zip
            *.sha256

# ========================
# Release
# ========================
  release:
    needs: [meta, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    if: |
      (github.event_name == 'push') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.create_release == 'true')

    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: 整理文件
        run: |
          mkdir release
          find artifacts -type f -exec mv {} release/ \;
          ls -lh release

      # 将多行 notes 写入临时文件，避免命令行转义问题
      - name: 准备 Release Notes 文件
        run: |
          cat > release_notes.txt << 'EOF'
          ${{ needs.meta.outputs.notes }}
          EOF

      - name: 发布 Release（自动覆盖）
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ needs.meta.outputs.tag }}"
          VERSION="${{ needs.meta.outputs.version }}"

          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release edit "$TAG" \
              --title "Release $VERSION" \
              --notes-file release_notes.txt
          else
            gh release create "$TAG" \
              --title "Release $VERSION" \
              --notes-file release_notes.txt
          fi

          gh release upload "$TAG" ./release/* --clobber
